# opera-test-target

Minimal Express todo API used for testing Opera's manager agent.

## Endpoints
- `GET /todos` — list todos
- `POST /todos` — create todo (`{ "title": "..." }`)

## Run
```bash
npm install && npm start
```

## Test
```bash
npm test
```
